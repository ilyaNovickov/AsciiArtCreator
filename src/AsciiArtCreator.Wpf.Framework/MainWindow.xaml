<Window x:Class="AsciiArtCreator.Wpf.Framework.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsciiArtCreator.Wpf.Framework"
        xmlns:Fluent="urn:fluent-ribbon"
        xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <BitmapImage x:Key="DefaultImage" UriSource="Assests/about.png"/>
        <!--<FontFamily x:Key="DefaultFont">
            <FontFamily.FamilyNames>
                Consolas
            </FontFamily.FamilyNames>
        </FontFamily>-->
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Fluent:Ribbon Grid.Row="0">
            <Fluent:RibbonTabItem Header="Основное">
                <Fluent:RibbonGroupBox>
                    
                    <Fluent:Button Header="Выбрать" SizeDefinition="middle" Command="{Binding SelectFileCommand}"/>
                    <Fluent:Button Header="Сохранить" SizeDefinition="middle"/>
                    <Fluent:Button Header="Экспорт" SizeDefinition="middle"/>
                    <Fluent:Button Header="Копировать" SizeDefinition="middle"/>
                </Fluent:RibbonGroupBox>
                <Fluent:RibbonGroupBox Header="Преобразование">
                    <Fluent:Button Header="Старт"/>
                    <Fluent:Button Header="Стоп"/>
                    <Fluent:SeparatorTabItem/>
                    <Fluent:RibbonToolBar Header="asd">
                        <Fluent:ComboBox x:Name="fontComboBox" 
                                         ItemsSource="{Binding Source={x:Static Fonts.SystemFontFamilies}}" 
                                         SelectedValue="Consolas" 
                                         IsReadOnly="True" 
                                         MinWidth="100"/>
                        <Button Content=" asd" Command="{Binding SelectFileCommand}"/>
                    </Fluent:RibbonToolBar>
                </Fluent:RibbonGroupBox>
            </Fluent:RibbonTabItem>
        </Fluent:Ribbon>

        <!--Основная панель-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0">
                <StatusBar DockPanel.Dock="Bottom">
                    <TextBlock Text="{}{@1+1"/>
                    <Slider x:Name="slider" Minimum="0.02" Maximum="10" MinWidth="100" Value="{Binding Scale}">
                        <behaviors:Interaction.Triggers>
                            <behaviors:EventTrigger EventName="ValueChanged">
                                <behaviors:InvokeCommandAction/>
                            </behaviors:EventTrigger>
                        </behaviors:Interaction.Triggers>
                    </Slider>
                    <ComboBox/>
                    <toolkit:SplitButton 
                        Content="{Binding Path=Scale}"
                        ContentStringFormat="{}{0:P}">
                        <toolkit:SplitButton.DropDownContent>
                            <Button>sad</Button>
                        </toolkit:SplitButton.DropDownContent>
                    </toolkit:SplitButton>
                </StatusBar>
                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                    <Image Stretch="None" Source="{Binding ImagePath, TargetNullValue={StaticResource DefaultImage}}" Grid.Column="0">
                        <Image.LayoutTransform>
                            <ScaleTransform ScaleX="{Binding Scale}" ScaleY="{Binding Scale}"/>
                        </Image.LayoutTransform>
                    </Image>
                </ScrollViewer>
            </DockPanel>
            <RichTextBox FontFamily="{Binding ElementName=fontComboBox, Path=SelectedValue, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" 
                         IsReadOnly="False" 
                         
                         Grid.Column="1">

            </RichTextBox>
        </Grid>
    </Grid>
</Window>
